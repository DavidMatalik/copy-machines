extends layout

block content
  h1= title
  p Welcome to #[em used Copy Machines], we buy and resell copy machines.

  if error
    p Error getting dynamic content.
  else
    p We have currently the following items and brands:

    ul
      li #[strong Items:] !{data.itemCount}
      li #[strong Brands:] !{data.brandCount}
